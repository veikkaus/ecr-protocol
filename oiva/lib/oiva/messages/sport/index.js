const { ebetMessages} = require('./ebet')
const { multiscoreMessages} = require('./multiscore')
const { scoreMessages} = require('./score')
const { vakioMessages} = require('./vakio')
const { winnerMessages} = require('./winner')

module.exports = {
  ebetMessages,
  multiscoreMessages,
  scoreMessages,
  vakioMessages,
  winnerMessages
}