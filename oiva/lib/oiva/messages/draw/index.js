const { aonMessages } = require('./aon')
const { lottoMessages } = require('./lotto')
const { milliMessages } = require('./milli')

module.exports = {
  aonMessages,
  lottoMessages,
  milliMessages
}
